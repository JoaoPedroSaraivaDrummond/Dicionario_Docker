2. docker ps

👉 Lista os containers que estão rodando no momento.

Opções úteis:

-a → lista todos os containers (ativos e parados).

-q → mostra só os IDs.

-ef → é um comando muito comum no Linux para exibir um 
instantâneo dos processos em execução no sistema, -e 
Este é um parâmetro que significa "every" (todos), -f
Este parâmetro significa "full" (completo).

Exemplo:

docker ps -a